apply plugin: 'com.android.application'

android {
    compileSdkVersion 17
    buildToolsVersion "29.0.1"

    defaultConfig {
        applicationId "org.zeroxlab.zeroxbenchmark"
        minSdkVersion 7
        targetSdkVersion 7
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
